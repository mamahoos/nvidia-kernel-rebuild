version: 2
updates:
  # Keep GitHub Actions versions current (checkout, shellcheck, shfmt).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels: [chore, dependencies]
